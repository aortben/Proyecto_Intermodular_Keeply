spring.application.name=keeply

# 1. Configuraci�n del Servidor
# 1. Configuracin del Servidor
server.port=8080

# 2. Conexin a MySQL (Asegrate de que la contrasea sea correcta)
spring.datasource.url=jdbc:mariadb://localhost:3307/keeply_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# 3. Configuracin de JPA (Para que cree las tablas solo)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# 4. JWT Configuration
jwt.secret=MiClaveSecretaParaKeeplyJWTMuyLargaYSegura2026ProyectoTFG
jwt.expiration=86400000

# 5. API Keys externas (TODO: Añadir las claves cuando estén disponibles)
# The Movie Database (TMDB)
api.tmdb.key=4da0b85272e93bad0f8ebc6c66adf077

# Jikan API (Anime/Manga) - Puede funcionar sin key pero es recomendable tenerla
api.jikan.key=

# RAWG API (Videojuegos)
api.rawg.key=

# Google Books API (Libros/Cómics) - Puede funcionar sin key pero es mejor tenerla
api.google.books.key=
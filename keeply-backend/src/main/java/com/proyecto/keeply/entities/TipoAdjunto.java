package com.proyecto.keeply.entities;

/**
 * Categoriza el tipo de fichero multimedia que el usuario ha adjuntado a una nota.
 * Sirve para que el frontend decida si renderizar una etiqueta <img>, <video> o <audio>.
 */
public enum TipoAdjunto {
    IMAGEN, VIDEO, AUDIO
}
